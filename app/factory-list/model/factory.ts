
export class Factory {

  code: string;
  name: string;
  region: string;

  constructor (code: string, name: string, region: string) {
    this.code = code;
    this.name = name;
    this.region = region;
  }
}
